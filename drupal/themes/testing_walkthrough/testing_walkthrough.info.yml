name: Testing Walkthrough
type: theme
description: 'Demo theme for visual regression testing concepts'
base theme: bartik
core: 8.x
libraries:
  - testing_walkthrough/test-lib

# This section is used by the contrib module, Component Libraries. It allows you
# to reference .twig files in your sass/ directory by using the Twig namespace:
# @components
component-libraries:
  components:
    paths:
      - components
      - templates
regions:
  header: Header
  content: Content
  sidebar_first: 'Sidebar first'
  footer: Footer